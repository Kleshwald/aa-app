# Project status

Snapshot на 2026-08-11. Обновляется после каждой существенной итерации.

## 🆕 2026-08-11 — Роль «Поддержка»: рабочее место, первая версия (локально)

Приоритеты владельца №3 и №4 («Процессы для роли Поддержка» + «Сообщения для роли Поддержка») — закрыты одним экраном, потому что для оператора это одна работа. Основание — `docs/SUPPORT.md` (архитектура от 2026-07-20): поддержка = ОДНА команда, ведёт и заявки 1С, и чат.

**Экран `/support`** (за гейтом `hubGuard`, вход с `/hub` — карточка «Рабочее место поддержки»). Master-detail: слева очередь, справа дело. Отдельных страниц нет сознательно — каждый переход «список → карточка → назад» стоит оператору места в очереди.

- **Одна очередь из двух источников** (заявки 1С + вопросы чата) — «тонкая агрегация» из SUPPORT.md §4: список указателей, не третья копия данных. Вкладки по оси **«чей мяч»** (Мои · У нас · У других · Все), не по виду заявки.
- **SLA капает, только когда мяч у нас** (2 ч «скоро» / 4 ч просрочка). Пока ждём агента или СК — часы не наши, иначе к вечеру очередь вся красная и слепнет. Просрочка = засечка строки + счётчик в шапке (единственное красное на экране).
- **Карточка заявки:** агент (ФИО/ИКП/телефон/территория/куратор) · договор · причины изменения · сумма возврата (расторжение, формула `[Р-ФТ-4]`) · номер убытка и выплата (УУ) · ход дела.
- **Действия:** взять на себя · запросить документы (пресеты по виду) · в работу · **передать в страховую** · завершить (итог по виду) · отклонить.
- **Две ленты с разными адресатами:** «Ответить агенту» (видит агент) и «Заметка для своих» (не видит) — разный фон и тег-подпись. Одно поле на двух адресатов — недопустимый риск (чужая кухня в кабинете агента).
- **Диалог:** ответ + передача во 2-ю линию **по теме** (расчёты · скоринг · пул/Автопомощник · техбаги). Куратора в эскалации нет — он параллельный канал отношений.
- **Живая петля (проверено CDP):** заявки демо-агента в очереди — те же объекты, что читает кабинет (`processes.fixture`). Запросил документы в кокпите → в кабинете «Ждут ваших действий» 2 → 3 + маркер в строке. ⚠️ Моки живут в памяти **вкладки**: ходить только ссылками внутри приложения (F5/вторая вкладка начинают историю заново).
- **Файлы:** `features/support/support.page.{ts,html,scss}`, `core/services/support.model.ts` + `support.service.ts`, `core/mock/{fixtures,handlers}/support.*`, роут `/support`, доп. док `/hub/doc/support` («Как работает поддержка»), `docs/SUPPORT.md` §8.
- **Долг:** очередь читает моки, а не 1С и вендора (блокеры из SUPPORT.md §7: чем 1С отдаёт открытые заявки наружу; вендор чата не выбран). Внутренние заметки и признак «ушло в СК» лежат рядом с заявкой в хендлере — в контракте у заявки нужны **два представления** (агентское и операторское). Нет: вложений, шаблонов ответов, отчётов и нагрузки по операторам, ролей внутри поддержки, звонков, отдельного места 2-й линии.
- Build ✅, lint ✅. Не зарелизено (локальные коммиты).

## ✅ Релиз 2026-07-23 — накопленные локальные коммиты + доки в origin; на /hub — раздел «Вариант на 1С 8.5»

Владелец: «На панель управления добавим блок с этим Вариантом на 1С 8.5, рядом — полезную информацию (брендбук для инженера), и выкатим в релиз всё незарелизенное с последних доработок базовой версии».

**Что ушло в origin (`git push` → GitHub Pages).** Впервые в проде — ~21 локальный коммит сессий 2026-07-13…16: сигнал внимания 6→3 слоёв, домен «Согласование» (сделка до полиса), flagship-fix переписки по заявке на 3G, 3 UX-варианта показа процессов (`?ux=v1|v2|v3`), статусы 1С в колонке «Статус» + бейдж вида заявки. Плюс доки, ранее только локально: база знаний легаси-системы (Белов, `docs/knowledge/**`, 14 файлов), развилка платформы (`DECISION-frontend-platform.md`), брендбук 1С 8.5 (`DESIGN-1C-85.md`), система взаимодействия 1С (`1C-interaction-system.md`), архитектура поддержки (`SUPPORT.md`), исходники заказчика (`.docx`).

**Новое на `/hub` — раздел «Вариант на 1С 8.5»** (форк-разведка): карточка кликабельного прототипа (внешний Artifact) + три дока в техпанели — **«Брендбук для инженера — 1С 8.5»** (`/hub/doc/design-1c`: шрифты, размеры, палитра hex, кнопки, поля, статусы; что платформа отдаёт и что нет — по `DESIGN-1C-85.md`, дизайн-закон Бахи), **«Почему 1С или Angular»** (`/hub/doc/platform-1c`), **«Система взаимодействия 1С»** (`/hub/doc/interaction-1c`). Lint/build зелёные (в бюджетах SCSS предсуществующие warning'и — норма). Баха-субагент на этой сборке снова не дошёл (сетевой обрыв) — брендбук сведён по его закону вручную.

## 🔀 Сессия 2026-07-16 — 3 версии показа процессов для сравнения (ЛОКАЛЬНО, `a72ec78`, НЕ в origin)

Владелец: «Давай сделаем 3 версии и дадим на них ссылки на доску управления». Собрано **одним рантайм-флагом** `?ux=v1|v2|v3` (`UxVariantService`, залипает в localStorage) — не три сборки: прототип живёт одним развёртыванием. Ссылки — секция «Версии интерфейса — процессы и уведомления» на `/hub`; dev-метка внизу слева показывает активный вариант и ведёт назад на /hub (видна только когда вариант выбран явно). Проверено CDP на 1366 (все три + hub). Ось различия — как заявки (изменение/расторжение/убыток) и сигнал «Ждут ваших действий» всплывают у агента:

- **v1 «Процессы в Сообщениях»** (ТЕКУЩАЯ = вариант B): сигнал в шапке → Сообщения/вкладка «Процессы» (список-указатель → страница договора). Бейдж «Сообщения» = чат + заявки.
- **v2 «Уведомления вверху»** (ПРЕДЫДУЩАЯ): сигнал → «Мои клиенты» с чипом «ждут вас»; Сообщения = только чат поддержки; бейдж = только чат. Заявки живут в договоре и таблице.
- **v3 «Переписка в Сообщениях»**: сигнал в шапке + переписка заявок синхронизирована в Сообщения — единый список бесед (поддержка + чат на клиента), лента заявки открывается прямо в Сообщениях с «Ответить по заявке» (черновик чистим лишь при успехе — правило 3G). ⚠️ Формат-кандидат: пересекает лок 152-ФЗ (зеркалирование `ProcessComment`) — здесь осознанный демо-вариант ДЛЯ СРАВНЕНИЯ.

**Решение pending** — владелец выбирает формат вживую (кандидат на телефонный RAT «куда рука тянется»). Долги: v3 deep-link из сигнала сразу в беседу ждущей заявки (сейчас список); мультивкладочность (variant в localStorage — сравнивать последовательно). Реализация: `messages.page` = `@switch(variant())`, чат вынесен в `#chatBlock` ng-template; main-layout — `attentionLink()` + `messagesCountsProcesses()` из UxVariantService. Память `project_ux_variant_comparison`.

## 🛠 Сессия 2026-07-15 — процессы, «язык внимания», хаб «Сообщения» (ЛОКАЛЬНО, 17 коммитов, НЕ в origin)

Всё собрано, сборка зелёная, проверено на живых экранах (headless Edge + CDP). **В прод НЕ запушено** — ждёт решения владельца о деплое. Параллельная сессия держит staged-индекс (cancel-флоу, knowledge-доки) — все мои коммиты scoped по путям.

**Домен «Согласование» (сделка ДО полиса):** модель `deal.model.ts` (phase/ballHolder/actionRequired — флаги бэкенда, не догадка фронта), страница `/deals/:id` (чей мяч → что дальше → акцепт тарифа → честная оплата: эквайринг у СК, статуса «оплачено» в модели НЕТ), мок `POST /deals` со `structuredClone` (иначе живая ссылка на фикстуру морозит сигналы). **Отдельный опросник-вход отменён владельцем** (`f66e238`) — запрос тарифа встроится ВНУТРЬ продукта (ОСАГО ЮЛ, Автопомощник). **Примеры сделок временно скрыты** (`DEAL_SEED_COUNT=0`) — в демо только процессы.

**Переписка по заявке — flagship fix (`92974f0`):** ответ терялся на 3G (черновик чистился ДО ответа сервера + `subscribe` без error → «я же написала»). Стало: окно = СНИМОК (не схлопывается на сбое поллинга), черновик чистим только при успехе, видимая ошибка, кнопка disabled (защита от двойного тапа). Вход-глагол «Ответить по заявке». Правило-закон: **фон не разрушает поверхность, в которой работает агент.**

**Язык внимания (редизайн, `4448682` + `88c3540` + `1592c58`):** сигнал «Ждут ваших действий» переехал из сайдбара в **шапку справа** (поверхность navy, тёплым горит только цифра — кремовая плашка давала 8:1 = «баннер»); нулевое состояние не исчезает («Всё сделано»); бейдж «Сообщения» красный→**янтарь** (единый язык: тёплый=«вас ждут», красный=ошибки); чип-фильтр → **тумблер** «Только ждущие меня»; фильтры уместились в **один ряд** (поиск компактный); маркер заявки — **под № полиса** словом+значком типа («Расторжение · В работе у страховой»); **коды ВИ/Р/УУ убраны везде** (владелец подтвердил: агенты говорят словами).

**Хаб «Сообщения» = единый ящик (`f4481fd`, вариант B):** вкладки **Диалоги** (чат) + **Процессы** (инбокс-список активных заявок, клик → страница договора). Бейдж = чат + ждущие заявки; сигнал в шапке = срочное подмножество, кликом ведёт в Процессы. Лок «вкладку в Сообщениях не делать» ПЕРЕФОРМУЛИРОВАН: навигация-указатель можно, зеркалирование данных в контур чат-вендора (152-ФЗ) — нельзя.

**Открытые долги / следующий ход:** (1) **push в прод** ~24 локальных коммита; (2) deep-link из инбокса прямо в окно заявки (сейчас лендинг на договор, окно вручную); (3) телефонный RAT на 5 агентах (Анвар: «куда рука тянется» — шапка или Сообщения); (4) контракт Case в `api-contract.yaml` (Белов, P0 хендоффа — процессов/сделок нет в контракте); (5) запрос тарифа встроить в ОСАГО ЮЛ + Автопомощник.

## ✅ Релиз 2026-07-13 — сигнал «Ждут ваших действий»: 6 слоёв → 3 (в проде)

Push `98fabd3..d138edd` в `main` → GitHub Pages. Build/lint зелёные. **Важно про гейт:** рабочее дерево содержит незакоммиченный WIP параллельной сессии (cancel-флоу, knowledge-доки), поэтому релиз-билд проверялся в **изолированном git-worktree на чистом HEAD** — он поймал недостающее поле `processSince` в `ClientRow` (мой коммит маркера зависел от него, а поле было в чужом WIP; добавил его отдельным коммитом `d138edd`). Без изолированной проверки на прод ушёл бы несобирающийся код.

Владелец после теста: сигнал «перебор и безвкусно». Разбор — Анвар (продукт) + Белов (система): одна работа чинилась 6 слоями, из них 5 на «Мои клиенты», а исходный RAT (заметность вне экрана) держал только сайдбар. Свели к **3 тихим слоям**:
- **Сайдбар** — единственный амбиентный детектор; клик → `/clients?awaiting=1` (чип-фильтр включается сам, таблица открывается уже списком).
- **Чип-фильтр «Ждут ваших действий»** — вынесен своей строкой над таблицей; чистая пилюля-переключатель (убраны глиф-чекбокс и счётчик — число уже в сайдбаре).
- **Маркер в строке** — только состояние словом («Нужны документы» / «В работе у страховой»), **без колокола** (он живёт только в сайдбаре); вид заявки и дата ушли на страницу договора.
- **Убрано:** зона-дубль `AttentionListComponent` (компонент удалён), подкраска/засечка строки, чип-сортировка «Сначала с заявками», чат из сигнала (у него свой бейдж «Сообщения»). Сайдбар и чип теперь одно число `awaitingPolicyCount`; `AttentionService` упрощён (без `items`/`count`/чата).
- Попутно Баха починил битый токен `--warning-200` (нет в системе) и контраст включённого чипа (4.37 → 4.87:1).
- **Системный долг (Белов, в бэклог, НЕ в этом релизе):** поллинг 5с как суррогат push (нет паузы на скрытой вкладке/ETag); «мяч у агента» должен приходить флагом бэкенда (`actionRequired`), а не выводиться из статуса на фронте (домена процессов нет в `api-contract.yaml`); дыры ЖЦ — доплата премии, `rejected` «подайте заново» (гасится как терминальный), нет SLA/старения.
- **Осталось (Баха, не срочно):** визуальный «шёпот» баннера в сайдбаре (сейчас — залитая карточка над меню).

Память `project_attention_signal` обновлена (зафиксирован разворот 6→3, перекрывает часть залоченного «единая лента»). Не в этом релизе: cancel-флоу и knowledge-доки параллельной сессии (её незакоммиченный WIP — уйдёт своим релизом).

## 🆕 2026-07-08 — Единый сигнал «Ждут ваших действий» (базовая версия — в проде, затем переработана 2026-07-13 выше)

Снятие RAT «агент 45+ пропустит одно из ДВУХ уведомлений» — бейдж «Сообщения» (общий чат, вендор, 152-ФЗ) и «ваш ход» по заявке (1С), который раньше был виден только на `/clients/:id`. Разбор — трио: Алюр (JTBD/варианты) → свёл Анвар (NMT), вёрстку прогнал Баха. Собрано code-first, проверено headless-логином через DevTools-протокол (индикатор+зона рендерятся, имя клиента джойнится). Build ✅, lint ✅. Память `project_attention_signal`.

- **Принцип (залочен):** объединить **ОБНАРУЖЕНИЕ** (одно место «где меня ждут»), развести **ДЕЙСТВИЕ** (клик уводит в СВОЮ систему). Агрегируем указатели/счётчики, НЕ тексты → два бэкенда не сливаются, «ответ там, где спросил» цел. Фактически — воскрешение «Срочных действий» удалённого Дашборда на сквозной поверхности.
- **Поправка Анвара (несущая):** сигнал в ПОСТОЯННОМ каркасе (виден на всех экранах, как бейдж «Сообщения»), а не только зоной на «Мои клиенты» (иначе RAT переносится на «вернётся ли днём на главную» — по профилю нет).
- **Реализация:** `AttentionService` (`core/services/attention.service.ts`, поллинг 5 c) = заявки `awaiting-docs` по ВСЕМ полисам (вне фильтра периода, `GET /processes/awaiting` → `handleListAwaitingProcesses`, джойн имени клиента) + непрочитанный чат одним пунктом. Общий `AttentionListComponent` (`shared/attention-list/`). Амбиентный триггер «Ждут ваших действий (N)» — **верх сайдбара** (`main-layout`, `.app-attention`); та же зона над таблицей «Мои клиенты». Callout на договоре и бейдж «Сообщения» — на месте (подкрепление). Пустое состояние спокойное: «Сейчас от вас ничего не ждут — всё под контролем».
- **Нейминг залочен:** сигнал зовётся **«Ждут ваших действий»** (прежнее «Требуют вас» отвергнуто владельцем — «так по-русски не говорят»: «требовать кого-то» = вызывать). Пункты говорят КОНКРЕТНО, что нужно, и не повторяют «ждут»: «Нужны документы: …» / «Нужен ваш ответ по заявке». Метафора «ваш ход» убрана (аудитория 45+ не обязана её считывать). Подпись в сайдбаре переносится на 2 строки → у `.app-attention` `min-height`, не `height`.
- **Решения владельца (2026-07-08):** сквозной индикатор (не только зона); бейдж «Сообщения» ОСТАВИТЬ + чат дублируется в индикаторе; **строки НЕ красить** (крас/жёлт/зел + коды ВИ/Р/УУ ОТВЕРГНУТЫ: конфликт с осью статуса полиса → страх «расторгнут», коды не расшифруют, фильтр `this-month` прячет старые полисы). Цвет внимания — **тёплый янтарь**, не красный; источник подписан словом («Заявка»/«Сообщение»).
- **Баха:** контраст счётчиков `warning-600`→`-700` (≥4.5:1), триггер выровнен по метрикам меню. **Открытый микро-вопрос:** красный бейдж «Сообщения» рядом с янтарём — унифицировать в янтарь или оставить (Jakob). Рекоменд. — оставить красный.
- **Главный RAT:** «баннерная слепота посреди задачи» — заметит ли агент смену индикатора, когда heads-down в ОСАГО. Заявка щёлкает в `awaiting-docs` посреди несвязанной задачи (мок продвигает через 9 c), без подсказки; замер time-to-notice. Провал-verbatim: «а я думала, всё в Сообщениях» / «полис расторгли?». ⚠️ **Анвар просил тестировать ДО фазы 2; владелец решил строить сразу.** Значит тест теперь проверяет ДВА слоя сразу (сквозной индикатор + маркер в списке) и «заметила бы она без маркера» мы не узнаем. На тесте смотреть: куда она смотрит ПЕРВЫМ (каркас или список).

### Фаза 2 — маркер в строке + чип-фильтр (сделано 2026-07-08)

- **Маркер заявки в строке** — ВТОРАЯ ось статуса, отдельная от колонки «Статус» (там статус ПОЛИСА). Пилл в ячейке **«№ полиса»** (заявка подаётся ПО полису) + вид заявки словом рядом («Изменение»/«Расторжение»/«Убыток», НЕ коды ВИ/Р/УУ). `awaiting-docs` → янтарный пилл «Нужны документы» + **янтарная засечка** `inset 3px` на `td:first-child` (полоса, НЕ заливка строки). Активные прочие → нейтральный «В работе». `done`/`rejected` → ничего.
- **Почему НЕ 9-я колонка** (Баха и замер сошлись): таблица **уже уезжает в горизонтальный скролл на 1366px** (нужно 1107px, есть 1007px). Колонка «Заявка» отобрала бы ≥160px у «Страхователя» → ФИО в 4 строки во ВСЕХ строках ради маркера в 5% из них, и сам маркер уехал бы за правый край. Пилл в 4-й из 8 колонок виден без прокрутки.
- **Грабли (важно):** Баха предложил `white-space: nowrap` на `.clients__policy-no` и на пилле — замер показал рост таблицы **1107 → 1202px** (overflow вырос вдвое). Номер полиса «ХХХ 0123456789» исторически переносится по пробелу, и это держит колонку узкой. Оба `nowrap` сняты → **1147px** (+40px к базе). Пилл переносится в 2 строки — сознательный размен: вертикаль в строке бесплатна (ФИО и так 3 строки).
- **Чип-фильтр «Ждут ваших действий (N)»** в тулбаре: toggle-кнопка (глиф-чекбокс + заливка + semibold + `aria-pressed` — три носителя состояния, не только цвет). **Снимает фильтр периода** (`awaitingOnly` в `ClientsQuery` → мок игнорирует `dateFrom/dateTo`), иначе `this-month` спрятал бы заявку на прошлогоднем полисе. Об этом говорим ПРЯМО строкой-пояснением, селект «Период» НЕ дизейблим. Чип скрыт при N=0. Счётчик — из того же поллинга `AttentionService` (без второго запроса).
- Данные: `ClientRow.processKind/processStatus` (джойн в `handleGetPolicies`), `AttentionService.awaitingPolicyCount`. Проверено CDP: маркер+засечка+чип рендерятся, чип фильтрует и корректно выключается, 1366/1600 замерены.
- **Открыто (Баха, не делал молча):** `.clients__status` = 14px, а пилл заявки 16px — перекос. Поднять статус-бейдж до 16px? Трогает ВСЕ строки таблицы. Ждёт решения владельца.
- **Отложено:** toast-перебой если тест покажет слепоту · экран для `rejected` (мёртвый статус — нет UI/сида) + его подключение в агрегатор · поповер-список из триггера (сейчас ведёт на зону) · плейсмент сайдбар↔шапка · токен `--surface-card` вместо `#ffffff`.

## 🆕 2026-07-07 — Регистрация агента «по ссылке» (локально, НЕ в проде)

Путь 2 онбординга (куратор кидает ссылку → агент регистрируется сам). Файлы: `src/app/pages/register.page.{ts,html,scss}`, общий `src/app/shared/onboarding-progress/*`, `public/images/logo-white.png`, карточка входа в `features/hub/hub.page.ts`. Собрано с Алюром (тезисы/JTBD) и Бахой (вёрстка), доводка — code-first со скриншот-циклом (headless Edge + DevTools-протокол для замеров/скринов на 1366/1600/390). Проверено на десктопе и мобильном; build ✅, register lint ✅ (в репо есть чужой давний lint-долг `JournalEntry` в `client-detail.page.ts` — не трогал).

- **Сплит-лендинг:** слева **светлая** промо-панель (`--brand-50`), справа форма. На <960px панель сверху (сжато), форма снизу.
- **Тезисы (владелец задал, перебили отбор Алюра):** «Одобрение 90% — настраивается для вашего района» · «Внесение изменений, расторжение и урегулирование убытков — через систему» (это наше главное отличие от конкурентов — на самом входе) · «Единая комиссия по всем компаниям — в 1 клик на карту». Заголовок «Зарабатывайте больше — без бумаг и отчётов».
- **«Стена партнёров»:** 8 СК на белой карточке = **лого + название** (владелец: одного лого мало, могут не узнать), вертикальные карточки 2×4, лого 52px / название 18px. Скрыта на мобильном (чтобы форма была близко).
- **Форма (упрощена по фидбэку владельца):** Фамилия · Имя/Отчество · **Адрес точки продаж — свободный ввод** (был справочник до района/select → владелец: «адрес это адрес, а не выбор района»; фикстуру `regions.fixture` удалил) · Форма работы (ФЛ/ИП/ЮЛ, **самозанятый НЕ на лендинге** — только после ФЛ) · Телефон. **Дату рождения и e-mail убрали** (владелец); ДР переносится на поздний шаг онбординга (оферта/реквизиты — итерация 2).
- **Флоу проверен end-to-end:** форма → «{Имя Отчество}, вы в системе» (общий `app-onboarding-progress`: Регистрация ✓ → Расчёты ✓ → Подпись оферты → Активен) + мягкие гейты (подписать оферту / добавить реквизиты — НЕ красным) → подпись оферты по SMS (демо: любой 4-значный код).
- **Чиненные баги вёрстки (важно на будущее):** (1) обрезался правый блок на десктопе — flex-элемент формы не сжимался; фикс `min-width:0` + flex-раскладка. (2) горизонтальный overflow на мобильном — у `.reg-card`/`.reg-hero` был `width:100%` **без `box-sizing:border-box`** (в проекте нет глобального reset — только точечный на инпутах); добавил `:host * { box-sizing: border-box }`. Замерял через DevTools-протокол (карточка 415px в колонке 375px). **Артефакт инструментария:** headless `--window-size=390` рендерит в ~500px и обрезает скрин до 390 → ложная «обрезка»; честный замер/скрин — только через `Emulation.setDeviceMetricsOverride`.

**Развилки владельцу (открыты):** где собирать дату рождения (оферта или реквизиты); «РГС» короткое vs «Росгосстрах» полное в стене партнёров; итерация 2 (гейты в кабинете + форма реквизитов), итерация 3 (кураторский флоу). **НЕ зарелизено** — только локальный коммит.

## ✅ Релиз 2026-06-25 — большой блок UX в проде (push в `main` → GitHub Pages; lint/build зелёные)

Зарелизено 2026-06-25 (предыдущий прод-релиз — страница договора 2026-06-18). В релиз вошло:
- **Лоадер расчёта (`calc-loader`)** — **минимализм**: центрированная сменяющаяся надпись + крупные лого СК (64px) + бегущая линия; «Скоринг» убран. **Финал (2026-06-24): без текста/галочки/зелёного** — лоадер плавно гаснет (`.calc__inner--done` opacity→0, hold 300мс) и появляются котировки; sr-only «Расчёт завершён».
- **«Здоровье»** — формы (параметры + оформление) **на Taiga/`app-field`**; первый экран по **Варианту 2** (заголовок слева+подзаголовок, карточки-герой, «Параметры · {продукт}», подписи сегментов, navy-пилюли). **Скидка клиенту переделана (2026-06-24):** единый блок справа `.hl-price` — «Цена клиенту» крупно + зачёркнутая старая + переключатель `.hl-toggle` «Полная / Скидка −15%» + «Клиент сэкономит N ₽»; фикс-цена → «Цена фиксированная». Сетка карточки 5→4 колонки. НЕ бегунок (эксперты против для 45+). Тиры 0/−15% (развилка: расширить до 0/−5/−10/−15).
- **«Магия» автозаполнения ОСАГО** — волна подсветки секций после «Ассистент, заполни!».
- **Окно выбора ОСАГО — БОЛЬШОЙ редизайн в ТАБЛИЦУ (2026-06-24, по макету владельца; с Бахой/Алюром/Анваром).** Перевернули прежнее «Итого-герой»: **сравнивают по цене ОСАГО** (главное число), «Итого» (= ОСАГО + сервис) — равного размера, вторично. **Заголовки колонок сверху** (Страховая компания · ОСАГО · Доп.сервисы · Итого) — как таблица 1С. Каждый факт — **под своей колонкой**: дата «Действует с…» под ценой ОСАГО, способ оплаты под «Итого», «Сегмент/Пул» под компанией. **Дата и оплата — per-СК** (в моке различаются; нельзя выносить в общий контекст — владелец). **«Лучшая цена» — у ВСЕХ с минимальной ценой ОСАГО** (часто несколько одинаковых; если все равны — не показываем); цены округлены до 100 ₽ (совпадения видны). **Доп.сервис — борд-лесс** (текст+цена+стрелка; пустой — «+ Добавить сервис» акцентом; обязательный — «Обязательно»). **«Урегулирование в ЛК Агента»** (наш процесс-отличие) — янтарным под РГС и СОГАЗ (`claimsInAgentLk`, per-СК). Строки — спокойный серый фон **без рамки** (рамку убрали — давала сдвиг колонок; состояния через inset box-shadow → шапка и строки совпадают пиксель-в-пиксель). Контейнер 1040→1200px. Сетка `64px 1fr 120px 240px 150px 140px`. **Залок памяти `project_quote_screen_conventions` обновлён.** Кнопки остаются navy (бренд; в макете были бирюзовые — это цвет макета). Отложенные владельцем развилки (кнопка «Оформить»=оплата, предустановленный доп, тиры скидки) — **не трогать**. ⏳ Здоровье ту же табличную конструкцию ещё НЕ получило (там цена+тоггл скидки) — кандидат на след. сессию.
- **Окно выбора (общее, ранее)** — a11y-фиксы (фокус CTA, адаптив 200%, тач-зоны «?», контраст); кегли ≥16px; фикс бага «?» (анимация входа `both`→`backwards`: `transform` не оставался и поповер не уходил под карточки ниже).
- **Сообщения — доработка чата (2026-06-24, трио Баха+Алюр+Анвар-метод):** исходящие пузыри облегчены navy→`--brand-100` (тёмный navy давил, инфлировал бренд); кнопка отправки = иконка + подпись «Отправить» (скрепка слева как в мессенджерах — оставлено осознанно), радиусы кнопок/поля → `--radius-md`, тач-зона удаления файла → 48px. **Шапка переделана:** два аватара (служба + куратор) + «Поддержка и ваш куратор Оксана Анатольевна» (тёплое имя-отчество) + честный статус, подложка `--brand-50`. **Залочено с владельцем:** (1) все переписки — в ОДНОМ чате (без каналов/вкладок); (2) поддержка — ОБОБЩЁННАЯ «Поддержка Agent Academy» (без выдуманного ФИО, аватар-иконка-гарнитура), куратор — именной (асимметрия «функция / отношения»); (3) аватары инициалы/иконка, БЕЗ фото (реальное лицо на мок-ответах = этический риск, публичных фото команды на сайте нет; фото куратора — опция только на реальном бэкенде с согласием). Ключ хранилища v3→v4. Память `project_messaging_architecture` обновлена. Ранее: чип вставляет текст, a11y/пустое состояние/авто-рост.
- **Единый «назад» — принцип «назад там же, где вперёд»** (memory `project_back_navigation_pattern`, обновлено 2026-06-24). Тип A (между роутами) = крошки `app-breadcrumbs` **ВВЕРХУ** (договор `← Мои клиенты`, без цепочки). Тип B (шаг во флоу) = `app-back-link` **ВНИЗУ**: ОСАГО результаты/Здоровье котировки — снизу слева с разделителем; Здоровье оформление — в paybar слева; лоадер — внизу по центру. ✅ договор/ОСАГО/Здоровье/лоадер; ⏳ **осталось:** `/hub/doc`, `/brandbook`, `/feedback` (крошки вверху), Обучение (спец-iframe); удалить мёртвые `.detail__crumbs` стили.
- **Шрифты подняты до ≥16px (2026-06-24):** ошибки в общем `app-field` (все формы), способ оплаты у «Итого», хинты/поповер/подписи ОСАГО; убраны хардкоды 11/12px.
- **Профиль → 3 вкладки (2026-06-24, с Бахой+Анваром):** `/profile` разбит на **Данные и реквизиты · Документы · Условия и КВ** (underline-табы как в Finance, ≥48px). Шапка-идентити (ФИО/ИКП/куратор/регион/статус) — **над вкладками**, видна всегда. Решение Анвара по NMT: профиль = справочник (редкий холодный триггерный заход), вкладки уместны (≠ форм-флоу). Деление по «соседству поводов», НЕ по варианту владельца: реквизиты не выносим отдельно (5 полей → пусто/тревога), «Условия/КВ» не отрываем от тарифного соглашения в «Документах». Названия вкладок = карта поводов (слово «реквизиты» в табе) — лечит RAT-риск «агент 45+ не заметит данные за вкладкой». Кросс-ссылка Условия→Документы. Техдолг Бахи закрыт в зоне правки: тач-цели кнопок ≥44px, `:focus-visible` на кнопках/вкладках. **Отложено:** токен `--card` вместо `#ffffff` (общий долг проекта); тест на 5 агентах холодным входом перед закреплением (если «теряют» вкладку → якорное оглавление).
- **Профиль: разбор легаси-вкладки 1С «Дополнительная информация» (2026-06-25, Баха+Алюр+Анвар по NMT; решения владельца).** Владелец показал карточку агента из 1С (7 вкладок) и спросил — копировать ли вкладку «Дополнительно». Вывод: **НЕ копируем 4-й вкладкой** (это карточка СК *об* агенте, не инструмент агента; вкладка заслужена, только если не пуста у типичного агента-одиночки). Раскладка: категория/территория/даты — дубли (уже в шапке/«Условия/КВ»); среднее ОСАГО/мес — метрика для «Финансов», не статичное поле; **фото офиса → «Документы»** (комплаенс части СК) — ✅ **сделано** (группа «Фото офиса», 3 слота со статусами/загрузкой); **сотрудники/субагенты → новый раздел сайдбара «Команда»** (Анвар: ИП со штатом = ценный по марже сегмент, X-сигнал; не поле профиля — отдельная фича с доступами/начислениями). Сотрудники — карточками (не 8-колоночная таблица 1С), пустые состояния плашкой. → **✅ реализовано** (см. след. пункт).
- **«Команда» — 4-я вкладка Профиля (2026-06-25).** Сперва сделали отдельным разделом сайдбара `/team`, затем **владелец перенёс в Профиль** (4-я вкладка после «Условия и КВ»; сайдбар вернулся к 8 пунктам, роут `/team` удалён). Реализация — панель-компонент `app-team-panel` (`features/team/team-panel.component.*`), встроена в `@case('team')` профиля; данные/мок `GET /agents/team` (`team.handler`/`team.fixture`/`TeamService`) сохранены. Контент: **карточки сотрудников** (аватар-инициалы + полное ФИО + бейдж категории; Телефон `tel:`/Email `mailto:`/Функции/Полисов в месяц/Адрес; ИКП моноширинно; «Изменить»/«Добавить» — заглушки) + **Субагенты** отдельной секцией с **пустым состоянием** «Работает без субагентов». Демо-данные близки к карточке 1С (Какоурова/Хунгуреева); демо-агент переведён в **ИП** (`agents.fixture` legalType:'ip') — штат консистентен, в профиле появился ОГРНИП. Карточки, НЕ 8-колоночная таблица 1С. Токены/≥16px/≥44px/`:focus-visible`. **Отложено:** доступы/начисления субагентам; **условная видимость вкладки** (сейчас видна всем; в идеале только ИП/ЮЛ — одиночке-ФЛ пуста); реальное добавление/редактирование вместо `alert`.
- **Пролонгация — редизайн (2026-06-24, с Бахой+Алюром, свёл Анвар по NMT):** убрана колонка **ИКП** из обеих таблиц (атрибут агента → Профиль, как в клиентах); кричащая material-заливка строк → **бейдж-статус с иконкой** в ячейке (как `.clients__status`, на токенах success/info/warning/error-50/700, контраст ≥4.5:1); **легенда** «цвета строк» убрана (дубль колонки); в **НСИС** убраны ИКП и блок **«Комментарии»** (футер удалён); убраны задизейбленные фильтры-заглушки и подпись «Ctrl+F»; консистентность с «Мои клиенты» (контролы 48px, текст 18px, шапка brand-50/700 без капса, зебра, единый скролл); телефон кликабельный (`tel:`). **Развилки владельцу (Анвар вынес, НЕ сделано):** действие «Продлить» → предзаполненный расчёт ОСАГО (крупный ход, нужен тест RAT на 5 агентах «доверяют ли прошлогодним данным»); убрать «Комментарии» и в «Мои» (→ «?»-поповер); h1 «Продление полисов» → «Пролонгация»; кликабельные метрики. **+2026-06-25:** метрики-цифры уменьшены (32px → `--text-xl` 24px, компактнее карточки); дефолт статус-фильтра «Оформленные и рассчитанные» → честное «Все статусы»; **кнопка «Скачать для прозвона»** в тулбаре — выгрузка текущего отфильтрованного списка в CSV (`;`-разделитель + BOM для Excel-кириллицы; ФИО/телефон/авто/полис/дата/СК/цена/статус) для обзвона клиентов. См. memory `project_prolongation_redesign`.
- **Фавиконки (2026-06-24):** `favicon.ico` (16/32/48) + `favicon-16/32.png` + `apple-touch-icon` + PWA-иконки 72…512 пересозданы со знаком Agent Academy (синяя «Δ» из `logo-mark.png`); `index.html` дополнен ссылками. Генерация — `System.Drawing` (без ImageMagick/sharp).
- **«Мои финансы» — доработка 3 вкладок (2026-06-25, Алюр+Баха+Анвар по NMT).** Раздел перестал быть «визуальным островом»: типографика/таблицы/статус-бейджи/кнопки/вкладки приведены к стилю «Мои клиенты» (18px, шапка без капса, строки 56px, зебра, статус-бейдж с иконкой, кнопки/вкладки ≥48px); убраны хардкоды (`44px`→`--text-3xl`, зебра `#fbfcfd`→`--gray-50`); **убран второй скролл** (закрыт долг `project_app_shell_scroll`). **Мои результаты:** порог ОСАГО — метка на шкале + подписи концов (плашку-статус и CTA «Оформить» владелец попросил убрать → HERO переверстан без них). **К выплате:** «Доступно к выплате» крупно сверху; после заявки — плашка успеха + «что дальше». **История:** «Акт №» реально скачивает демо-PDF (была мёртвая ссылка). **Развилки владельца — решены «оставить как есть»:** Перестраховочный пул, 4 KPI «Целевой сегмент», личный КВ в ₽ НЕ показываем (политика непоказа КВ).
- **Единый стиль заголовков страниц (2026-06-25, решение владельца).** Все h1 страниц — один вид: `text-2xl` / semibold / `--brand-700` / **по центру** (как «Мои клиенты»). Раньше часть была слева и светлее (`--header-bg`). **Канон — ЦЕНТР** (владелец выбрал из «всё слева / всё по центру»).

**Субагенты:** дизайн — **Баха** (`baha`), продукт — **Алюр** (`alur`), старший продакт — **Анвар** (`anvar`, Next Move Theory; ревьюит/сводит Баху+Алюра). Со след. сессии правки Бахи/Алюра прогонять через Анвара.

**Зарелизено 2026-06-25** (push в `main` → GitHub Pages). **Следующие шаги (после релиза):** (1) проверить прод после деплоя (~3 мин). (2) Привести экран котировок **Здоровья** к табличной конструкции ОСАГО (с поправкой на тоггл скидки). (3) Доделать «назад» на hub/feedback/обучении; удалить мёртвые `.detail__crumbs`. (4) Отложенные развилки через Анвара: «Оформить»=оплата, тиры скидки Здоровья, условная видимость вкладки «Команда» (только ИП/ЮЛ), реальное добавление сотрудников/субагентов.

## Что готово (можно показывать)

| Что | Файлы | Коммит |
|-----|-------|--------|
| Bootstrap проекта (Angular 20 + Taiga 5 + Tailwind + PWA + ESLint/Husky) | `angular.json`, `package.json`, `eslint.config.mjs`, `tailwind.config.js`, `src/styles/*` | `d8b5772` |
| Mock-слой: HttpInterceptor + Faker fixtures (seeded), handlers для auth/agent/dashboard/policies | `src/app/core/mock/**` | `d8b5772` |
| API-слой: ApiClient, auth/error/mock interceptors, генерация типов из OpenAPI | `src/app/core/api/**`, `src/app/core/models/api.generated.ts` | `d8b5772` |
| Login страница (phone-first, глаз пароля, забыли пароль, брендовая палитра) | `src/app/pages/login.page.{ts,html,scss}` | `eac638a` |
| MainLayout: фикс. navy header — **нейтральный силуэт-аватар + полное ФИО + территория/район строкой под именем** (профиль тянется из `/agents/me`, не из снимка входа); меню Профиль·Настройки·Выйти (48px); 8-пунктовый sidebar (бейдж непрочитанных на «Сообщениях», крупный блок «Наши группы»). **Один скролл:** шапка/сайдбар фиксированы, прокрутка только в `.app-content` (sticky-заголовки таблиц липнут к нему) | `src/app/layouts/main-layout.component.{ts,html,scss}`, `src/app/core/services/auth.service.ts` | `2874a00`, ревизия 2026-06-18 |
| **Главная «Мои клиенты»** (открывается после входа; Дашборд удалён): **8 колонок** (ИКП/Куратор убраны — атрибуты агента); объект страхования по продукту (авто+госномер / Здоровье / Недвижимость); **фильтр дат** (Сегодня с временем · месяц/квартал/год · произвольный период с Taiga-календарями, реальная фильтрация в моке); **сортировка** по Дате и Цене (▲/▼); статусы Оформлен/Черновик; поиск (debounce 300мс, серверный); sticky-заголовки; подписи 16px, без CAPS | `src/app/features/clients/clients.page.{ts,html,scss}`, `client.service.ts`, `policies.handler.ts`, `policies.fixture.ts`, `shared/iso-day.transformer.ts` | `8550938`…, ревизия 2026-06-18 |
| **Сообщения = чат поддержки агента** (НАШ UI, не виджет): полноэкранный чат, одна тема/две роли (Поддержка+Куратор) с подписями и аватарами, композер с вложениями, быстрые подсказки, мок `ChatService` (localStorage) | `src/app/features/messages/messages.page.{ts,html,scss}`, `src/app/core/services/chat.service.ts` | `9c9fdca` |
| **Страница договора** `/clients/:id` («Информация о договоре») — **редизайн с Бахой+Алюром** (принципы А. Дороничева): хлебные крошки `← Мои клиенты` (18px), 2-колоночный layout. **Резюме** — лого СК + продукт + название СК; **блок стоимости** (ОСАГО + допы, номера полисов рядом с ценой строкой → «Итого, клиент оплатил»); реквизиты + «Ещё» (для ОСАГО — только водители). **Документы видны сразу** (все выбраны по умолчанию, «Выбрать все», счётчик), кнопка **«Скачать»** (тянет демо-PDF `public/docs/`) + **QR** (`qrcode`, абсолютная ссылка на полис — клиент сканирует и забирает; печать — из PDF клиентом, кнопок «распечатать»/«на телефон» нет). **Процессы** (внести изменения · расторгнуть · убыток) и **кросс-сел** (докупить до 3-го допа, цены допов) — на виду; порядок колонок зависит от сценария (горячий `?issued=1`: допродажа сверху; холодный: процессы сверху). Адаптирована под продукт (не-ОСАГО без ТС; Здоровье — Полис/Таблица выплат/КИД) | `src/app/features/clients/client-detail.page.{ts,html,scss}`, `src/app/core/services/client-detail.service.ts`, `policies.handler.ts`, `public/docs/*.pdf` | `b7258ac`, `7749f44`, _этот релиз_ |
| **Оформление → договор (ОСАГО + Здоровье):** после имитации эквайринга (≈3 c) создаётся договор `POST /policies` (надёжный мок, без random-fail) и открывается его страница `/clients/:id`. Договор кладётся в начало in-memory списка | `policies.handler.ts` (`handleCreatePolicy`), `policies.fixture.ts` (`createPolicy`), `mock.interceptor.ts`, `client-detail.service.ts` (`create`) | `7749f44` |
| **Расчёт ОСАГО**: форма (страхователь/ТС/водители) → анимация опроса СК → **табличный экран котировок** (выровненные колонки, лого 64px, бейдж «Сегмент»/«Перестраховочный пул», «?»-поповер расчёта по коэффициентам, «Итого»-герой + способ оплаты, чипы доп.сервисов: изменяемый/обязательный) → допсервисы в модалке → оплата → успех | `src/app/features/osago/osago.page.{ts,html,scss}` | `4c3bee2`, `15658e6`, `9ad93fa` |
| **Пролонгация**: вкладки «Мои пролонгации» (статистика + поиск/фильтр) и «Поиск в НСИС» (ФИО/госномер/ВУ) | `src/app/features/prolongation/prolongation.page.{ts,html,scss}`, `prolongation.service.ts`, `prolongations.handler.ts`, `prolongations.fixture.ts` | `9474c03` |
| **Обучение**: наша компактная разводящая (6 продуктов с описаниями) → детальные Tilda-страницы открываются в iframe внутри кабинета; навигация «К обучению» + «В новой вкладке» | `src/app/features/learning/learning.page.ts` | `36137c0` |
| Блок «Наши группы» внизу sidebar — Telegram + ВКонтакте | `src/app/layouts/main-layout.component.{html,scss}` | `6774407`, `b2ec7e9` |
| **Здоровье**: первая страница — **3 продукта-карточки с иконками** (Несчастный случай / Спортсмены / Укус клеща) + параметры → общий лоадер расчёта → котировки **в стиле ОСАГО** (без radio: выбор кнопкой «Продолжить» на карточке; «Лучшая цена»; «?»-поповер) с **управлением ценой «Скидка из комиссии»** (0 / −15%; у части СК цена фиксированная, напр. Антиклещ-Ренессанс) → оформление (название продукта сверху, лого СК в панели оплаты, «Добавить/Удалить застрахованного») → эквайринг → страница договора. СК: НС/Спорт — Ренессанс/Согласие(/Югория), Клещ — Ренессанс(фикс)/Югория | `src/app/features/health/health.page.{ts,html,scss}` | `b9ed16f`, `2f197d5`, `6deb05e`, `7749f44` |
| **Shared-компоненты**: `app-insurer-logo` (лого СК + монограмма-fallback, `--ins-logo-size`), `app-addon-icon` (линейные иконки доп.продуктов), **`app-calc-loader`** (общий экран ожидания расчёта ОСАГО+Здоровья: B2B-eyebrow, бегущая строка, лого СК на шагах, финиш «Готово», `prefers-reduced-motion`) | `src/app/shared/insurer-logo/*`, `src/app/shared/addon-icon/*`, `src/app/shared/calc-loader/*` | `066ff31`, `cac401a`, `5a96841` |
| 2 заглушки для остальных sidebar пунктов | `src/app/features/{mortgage,finance}/*.page.ts` | `46a6b73` |
| **Панель проекта `/hub`**: гейт-вход `/hub-unlock` оформлен как вход в платформу (любой телефон + пароль `6767`); новая секция «Версии и платформы» (мобильная/планшет/React — заглушки-намерения с доками `/hub/doc/{mobile,tablet,react}` + `docs/PLATFORMS.md`); внешний вход «ЛК поддержки» (Webim); «Обычный вход» помечен «доступно» | `src/app/pages/hub-unlock.page.ts`, `src/app/core/guards/hub.guard.ts`, `src/app/features/hub/{hub.page.*,doc.page.ts}`, `docs/PLATFORMS.md` | _локально_ |
| **Доска обратной связи `/feedback`** (публичная, без гейта — ссылку даём тестировщикам): **форум** с разделами (Клиентский путь/дизайн · Архитектура/тех · Продукт/роадмэп/бэклог), рубрикатором страниц внутри «Клиентского пути», тип(Идея/Вопрос/Проблема/Другое), **ответами** (ветки) и **лайками**; скриншот (file или Ctrl+V), все видят всё. **Модерация:** удаление через серверную функцию `delete_feedback(pass)` (пароль НЕ в бандле), на доске — режим «Модерация». Бэкенд — **Supabase** (PostgREST+Storage+RPC) через нативный `fetch` мимо моков; RLS public read/insert, лайки/удаление через SECURITY DEFINER функции. Проверено end-to-end (запись/чтение/скрин/лайк/удаление через API). Настройка+SQL — `docs/FEEDBACK.md`, ключи в `feedback.config.ts`. ⚠️ UI-удаление в браузере на момент релиза не довалидировано (бэкенд работает) — проверить в начале след. сессии | `src/app/features/feedback/*`, `docs/FEEDBACK.md`, `src/app/features/hub/*`, `src/app/pages/hub-unlock.page.ts` | `2170fdd`…(релиз) |

## В работе сейчас

**✅ ОСАГО: форма полностью переработана на Taiga UI / общий `<app-field>` (готово, локально).** За сессию 2026-06-16 (~25 коммитов, до `e7a1a59`) форму довели с дизайнером **Бахой** и ПМ:
- Все поля — через общий `<app-field>` (подпись сверху, стабильный слот ошибки/подсказки, ширины tiny/narrow/small/grow/wide). Календарь дат = `<tui-calendar *tuiDropdown>`. «Крестик» очистки выключен.
- Порядок секций: **ТС · Страхователь · Собственник · Водители · Параметры и контакты** (последняя — в конце, перед расчётом). У каждой — бейдж «Заполнено/Не заполнено».
- **ТС:** строка поиска (Гос.номер/VIN/Кузов + «Найти»-мок) + тумблер «Произвольная марка/модель» (справочник марок/моделей ↔ свободный ввод; смена марки сбрасывает модель). Блок «Идентификатор» (тип + поле) и сегмент «Документ ТС» — в одной строке; «С прицепом» в шапке.
- **Собственник/Водитель:** при совпадении/выборе источника поля сворачиваются, иначе раскрываются. Водители: сегмент «Ограниченный список / Без ограничений», «Добавить/Удалить водителя» (макс 5), пилюли источника Страхователь/Собственник/Иное лицо (предзаполняют ФИО+дату; занятый источник скрыт; собственник=страхователь не дублируется); ФИО+дата видны всегда; «Тип документа» В/У.
- **«Серия и номер»** (паспорт и В/У) — одно поле с маской `1234 567890` (maskito); адрес — широкий.
- Ошибки обяз. полей текстом+иконкой; «Рассчитать» блокируется при невалидной форме. Кнопка **«Ассистент, заполни!»** (случайные данные) в нижней панели.
- Паттерны и грабли — в memory `project_taiga_form_pattern.md`. **Зарелижено в демо** 2026-06-17 (пуш в `main` → GitHub Pages).

**🎨 Дизайн/продукт-инфраструктура:** `DESIGN.md` в корне репо — закон UI (подписи сверху, токены, ≥16px/≥44px, ошибки текстом+иконкой, единые компоненты). Дизайнер — субагент **Баха** (`.claude/agents/baha.md`); продакт-менеджер — субагент **Алюр** (`.claude/agents/alur.md`, добавлен 2026-06-18; «что и зачем» через JTBD + профиль аудитории). Оба локально, вне git. Залоченное правило: **имена людей — полным ФИО с отчеством** (не обрезать). Разделы на `/hub` синхронизированы. См. memory `feedback_design_flow.md`.

**Дальше по формам:** распространить `<app-field>` и эти паттерны на Здоровье и др.; общие `app-button`/`app-card`; настоящие маски телефона/госномера; токен `--card` вместо `#ffffff`.

**Система сообщений / поддержка — см. [docs/MESSAGING.md](./docs/MESSAGING.md)** (полный хендофф).
- ✅ Агентский чат (наш UI, мок) — готов.
- ⏳ **Активное решение: выбор РФ-бэкенда** (152-ФЗ). Демо шортлиста: **Webim** + **edna/Carrot quest**. Chatwoot Cloud отклонён для прода. Кастомный адаптер строим ТОЛЬКО после выбора (мок держит UX независимым).
- Дальше по сообщениям: дашборд куратора → звонки (Voximplant/ВАТС) → канал МАКС.

## Дизайн-конвенции экрана котировок (ОСАГО + Здоровье) — зафиксировано с ПМ+дизайнером

Экраны выбора СК унифицированы. Решения **залочены** (не переоткрывать без причины):

- **Табличное выравнивание.** Карточки — одинаковые фиксированные колонки (НЕ subgrid: конфликт с padding карточек). ОСАГО-сетка `64px minmax(0,1fr) 130px 320px 160px`. Контейнер: ОСАГО 1040px, Здоровье 880px, по центру.
- **«Итого» — главное число** (28px navy). Цена ОСАГО — вторичная (24px серая): ранг виден по сортировке и «?». Два числа не конкурируют.
- **«?» у цены → поповер с расчётом** по коэффициентам (ОСАГО: ТБ×КТ×КБМ×КВС×КМ×КО; Здоровье: сумма×тариф×срок×категория×кол-во−КВ). Click-toggle, закрытие по клику-вне/Esc.
- **Сортировка:** Сегмент сверху, Перестраховочный пул — всегда снизу; внутри групп по цене. Самая дешёвая — бейдж «Лучшая цена» + зелёный акцент, выбрана по умолчанию.
- **Способ оплаты** у «Итого» (ОСАГО) выводится из переключателя плательщика: Физлицо/ИП → «Оплата картой», Юрлицо → «Оплата по счёту».
- **Доп.продукт всегда предустановлен.** Изменяемый чип — 44px с шевроном-в-круге (акцент); обязательный (пул) — залочен (замок + «Обязательно», в модалке нельзя отключить); пустой — пунктир «Добавить сервис».
- **НЕ показывать КВ агента в рублях** на экране (у проекта другая система мотивации). Подвал-«полоса доверия» убран.
- **Экран ожидания расчёта — общий `app-calc-loader`** (ОСАГО ~40 c, Здоровье медленнее, чтобы читалось). Тон B2B (НЕ «подбираем/подобрали»): eyebrow «Расчёт ОСАГО»/«Расчёт стоимости», бегущая строка, лого СК на шагах с компаниями, финиш — «Готово». Какая СК ответила/в каком порядке/по какой цене — НЕ раскрываем (алгоритм вывода под капотом).
- **Здоровье выбирает БЕЗ radio** — выбор кнопкой «Продолжить» на каждой карточке (как «Оформить» в ОСАГО). Управление ценой = чипы **«Скидка из комиссии»** (0 / −15%); у части СК — «Фиксированная цена» (флаг `discountable`).
- **Первая страница Здоровья — карточки-продукты** (иконка-бейдж + название + строка «что покрывает»), выбранная = акцентная рамка + подложка + галочка. НЕ текст-вкладки.
- **После оплаты — страница договора `/clients/:id`**, не список и не отдельная карточка успеха.

Подробности и обоснование — в memory `project_quote_screen_conventions.md` и `project_issue_to_contract_flow.md`.

## Бэклог приоритезированно

**Приоритеты дальше (владелец, 2026-06-25):**
1. **Регистрация агента** — вход с регистрацией для ролей «Агент» и «Куратор».
2. **Процессы для агента** — внесение изменений (ВИ), расторжения, убытки.
3. ~~**Процессы для роли «Поддержка»**~~ + 4. ~~**Сообщения для роли «Поддержка»**~~ — первая версия сделана 2026-08-11 (`/support`, см. блок вверху). Дальше — подключение к реальным источникам, а не новый UI.
5. **ОСАГО для юрлица (ЮЛ)**.
6. **Информация о котировке** — детали расчёта по предложению.
7. **Автопомощник**.
8. **Система мотивации**.
9. **Мобильная вёрстка**.
10. **Роль «Администратор», настройки**.

**Отдельные задачи (не в нумерованных приоритетах):**
- **Сообщения** — выбрать РФ-бэкенд (152-ФЗ) и протестировать (основа приоритета №4).
- **Мои клиенты** — логика работы с процессами (связано с приоритетом №2).
- **ОСАГО** — поиск ТС/клиента (UI-блок госномер/VIN/кузов готов, нужен реальный источник).
- **Команда** — раздел сайдбара для ИП со штатом (решено в V1; детали в хендофф-блоке выше).
- **Пролонгация** — развилки после редизайна (см. memory `project_prolongation_redesign`).
- **/forgot-password** — страница-заглушка (login ссылается).
- **PATTERNS.md** — cookbook «как сделать новую страницу/сервис/handler».

**Сделано:** Профиль агента + раздел «Документы», «Мои результаты» на реальной мотивации, виджет ипотеки polis.online, техническая панель `/hub` (заменила идею `/system` styleguide). Ранее: Расчёт ОСАГО, Пролонгация, Detail-страница договора.

## Известные проблемы / технический долг

- **Перезагрузка страницы (F5)** теперь восстанавливает agent profile из localStorage. Если переименовать поля в `AuthenticatedAgent` — старые ключи в localStorage могут оказаться без новых полей у уже залогиненных тестировщиков (нужен relogin).
- **api-contract.yaml** описывает `login` как «ИКП агента или email» — устарело, нужно обновить на «номер телефона» при следующем правке контракта.
- **Стили компонентов разбросаны по `.scss` страниц.** Пора вынести повторяющиеся блоки в `shared/components/` (`app-page-header`, `app-status-badge`, `app-data-table`; экран котировок ОСАГО/Здоровье — кандидат №1 на `app-quote-row`). `osago.page.scss` раздулся до ~19.6 kB (богатый экран: форма+загрузка+котировки+модалка+поповер+оплата+успех), из-за чего `anyComponentStyle` error-бюджет в `angular.json` поднят до **24 kB** (warning — 8 kB как напоминание). Историю: 12 → 20 → 24 kB.
- **Два разных паттерна поиска.** Таблица клиентов — серверный поиск через `toObservable` + `debounceTime` + `switchMap` (правильный). Пролонгация — клиентская фильтрация через `searchTick`-сигнал, который computed `myFiltered` читает, чтобы пересчитаться при вводе в `FormControl`. При выносе в `shared/` свести к одному подходу.
- **Оформление создаёт договор только в памяти.** `POST /policies` (`createPolicy`) кладёт полис в начало массива `policies` (in-memory, на сессию). После оплаты сразу открывается `/clients/:id` — работает в рамках одного прохода. **При F5 на странице договора полис «теряется»** (моки регенерируются при перезагрузке → «договор не найден»). Если демо с перезагрузками — нужен persist (localStorage). Имитация эквайринга — `setTimeout` 3 c; счётчики лоадера (`CALC_TOTAL_MS = 40_000` в ОСАГО, `HL_STEP_MS` в Здоровье) демонстрационные.
- **Список «Мои клиенты» не знает про новые продукты Здоровья.** Свежий договор появляется сверху, но колонка продукта/объекта рендерится по старой логике (тип `NS`/`TICK`, без авто). Причесать при доработке списка.
- **`DESIGN_SYSTEM.md`**: палитра синхронизирована с `tokens.scss` (брендовый navy+голубой, `--header-bg`, 3 роли цвета). Компонентные паттерны (шапка с аватаром, чат) добить при `/system` styleguide. Источник истины по токенам — `src/styles/tokens.scss`.
- **PWA service-worker** включён только в production-build. В dev — отключён через `isDevMode()`.
- **`@taiga-ui/styles/taiga-ui-theme.less` не активирует light-палитру через `:root` сам** — пришлось добавить атрибут `tuiTheme="light"` на `<tui-root>` в `app.ts`. Если в будущем добавлять dark mode — это точка переключения.

## Vite dev-server: критичный нюанс окружения

**Проект ДОЛЖЕН лежать в пути без пробелов и кириллицы** (`D:\aa-app\`, не `D:\Архив Claude\Прототип 3.0\`). Иначе Vite генерирует `polyfills.js` с unencoded путями (`/@fs/D:/Архив Claude/...`), браузер не может загрузить zone.js, рендерится белая страница. Junctions и `NODE_PRESERVE_SYMLINKS=1` не помогают (Vite внутри использует `fs.realpath`).

**`ng serve` без `--host` биндится только на IPv6 (`::1`)** на этой машине, а Windows резолвит `localhost` в IPv4 (`127.0.0.1`) первым → «сайт не открывается». Запускать **`ng serve --host 0.0.0.0 --port 4200`** (слушает IPv4) или открывать `http://127.0.0.1:4200/` напрямую.

**esbuild EPIPE после долгой сессии / новых зависимостей.** Если добавить новую библиотеку (напр. начали использовать Taiga) в долго работающий `ng serve`, esbuild может упасть (`The service is no longer running: write EPIPE`) и отдавать битый чанк — страница «не открывается». Лечится **перезапуском dev-сервера** (чистая переоптимизация зависимостей). Признак в логе сервера — строка с `EPIPE`.

## Браузер для проверки

- ✅ **Microsoft Edge** или Chrome — F12 работает, никаких блокировок
- ⚠️ **Yandex Browser** — F12 иногда блокируется (политика Kaspersky), но визуально страница работает
- ⚠️ **Kaspersky Endpoint Security** на машине инжектит свой `main.js` в HTTP-трафик (включая localhost). Не критично для функциональности, но может ломать lazy chunks при некоторых конфигах защиты

## Релиз / доступ (закрытое демо)

- **Прод:** https://kleshwald.github.io/aa-app/ (репо `Kleshwald/aa-app`, public).
- **Хостинг:** GitHub Pages через Actions (`.github/workflows/deploy.yml`) — пуш в `main` собирает **demo-сборку** (`--configuration demo`: прод-оптимизация + моки, `environment.demo.ts`, `mockErrorRate:0`) и публикует. SPA-fallback (`404.html`), `.nojekyll`, `base-href` из имени репо. Ассеты — относительными путями (работают под подпапкой `/aa-app/`).
- **Вход в приложение** (мок `auth.handler.ts`):
  - Логин — **любой номер телефона** (не сверяется, нужен лишь непустой).
  - Пароль: **`6767`**.
  - После входа агент попадает на **«Мои клиенты»**.
- **Панель `/hub`** — за гейтом-входом (`/hub-unlock`): логин — **Имя** (любое, сохраняется в `aa_user_name` и подставляется на `/feedback`), пароль — **`6767`** (клиентский гейт `hub.guard.ts`, sessionStorage; не настоящая защита — пароль в бандле). Креды на самой панели не показываем.
- **Доска `/feedback`** — публичная (без гейта), бэкенд Supabase. Пароль модерации (для удаления записей) — **`feedback2026`**, проверяется серверной функцией `delete_feedback`, в бандле его нет; меняется через `create or replace` в Supabase. Подробности и SQL — `docs/FEEDBACK.md`.
- Ссылка не индексируется (`noindex` в `index.html`).
- **Включить Pages один раз:** создать репозиторий → запушить `main` → Settings → Pages → Source: GitHub Actions.

## Команды-шпаргалка

```bash
cd D:\aa-app

npx --no-install ng serve --host 0.0.0.0 --port 4200   # dev server (см. нюанс IPv6 ниже)
npx --no-install ng build                    # production build
npx --no-install ng lint                     # ESLint
npm run generate:api                         # пересобрать src/app/core/models/api.generated.ts

git status                                    # husky хуки активны — lint-staged + commitlint на commit
```

## Текущий счёт работы

- **Готовых страниц:** 8 (login, Мои клиенты, Detail договора, ОСАГО, Пролонгация, Сообщения/чат, Обучение, Здоровье) + 2 заглушки (Ипотека, Финансы) + Профиль/Дашборд в бэклоге
- **Поток оформления сквозной:** ОСАГО/Здоровье → лоадер → котировки → оплата → **страница договора** (мок создаёт запись)
- **⚠️ Обучение (iframe Tilda):** если `agentacademy.ru` отдаёт `X-Frame-Options`/`frame-ancestors`, iframe не отрисуется. Проверить в браузере; если блок — включить разрешение в Tilda или сменить подход. Есть запасная ссылка.
- **Lazy chunks (prod, gzip):** osago ~13 KB, health ~6.7 KB, prolongation 5.2 KB, main-layout 4.5 KB, client-detail 4.3 KB, clients 4.4 KB, messages 4.2 KB, login 2.7 KB
- **Initial bundle (prod):** ~1.19 MB raw (не оптимизирован для production метрик ещё; budget warning ожидаем)
- **Сборка/линт:** `ng build` ✅ (warnings по CSS-бюджету), `ng lint` ✅ чисто
